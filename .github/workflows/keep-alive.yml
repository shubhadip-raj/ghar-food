name: Keep Vercel Alive

on:
  schedule:
    - cron: "*/15 * * * *" # Every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Health API
        run: curl https://ghar-food.vercel.app/api/health